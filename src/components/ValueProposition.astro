---
/**
 * ValueProposition — tres valores premium que definen la experiencia JW.
 */
import glicerinaImage from '../assets/glicerina.jpeg';
import curadoImage from '../assets/curado.jpeg';
import botanicaImage from '../assets/botanica.jpeg';

const values = [
	{
		title: 'Glicerina Natural Conservada',
		desc: 'Retenemos el 100% de los agentes humectantes que el jabón industrial descarta, para una limpieza que hidrata en lugar de resecar.',
		image: glicerinaImage,
		alt: 'Textura cremosa de glicerina natural',
	},
	{
		title: 'Curado de 4 a 6 Semanas',
		desc: 'Cada lote reposa paciente hasta lograr una barra suave, duradera y con un pH equilibrado que respeta tu piel.',
		image: curadoImage,
		alt: 'Jabón en proceso de curado artesanal',
	},
	{
		title: 'Botánica Pura y Sostenible',
		desc: 'Aceites vírgenes, mantecas, arcillas y extractos florales, con empaques reciclables.',
		image: botanicaImage,
		alt: 'Ingredientes botánicos puros',
	},
];
---

<section class="relative mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8">
	<div class="mx-auto max-w-2xl text-center">
		<span class="eyebrow inline-flex items-center gap-3">
			<span class="h-px w-8 bg-hunter-green-600"></span>
			Por qué JW
			<span class="h-px w-8 bg-hunter-green-600"></span>
		</span>
		<h2 class="mt-4 font-serif text-4xl font-medium sm:text-5xl">
			El Cuidado Premium que tu Piel <span class="italic text-hunter-green-600">Merece</span>
		</h2>
		<p class="mt-4 text-base leading-relaxed text-pine-teal-400">
			Cada producto es resultado de un proceso artesanal lento y cuidadoso. Así garantizamos
			calidad, pureza y una experiencia sensorial inolvidable.
		</p>
	</div>

	<div class="mt-14 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
		{values.map((value) => (
			<div
				data-reveal-grid
				class="group relative overflow-hidden rounded-4xl border border-pine-teal/10 bg-ecru p-1.5 shadow-soft transition-all duration-300 hover:-translate-y-1.5 hover:shadow-lift"
			>
				<div class="relative aspect-[4/3] w-full overflow-hidden rounded-[1.7rem] bg-ecru">
					<img
						src={value.image.src}
						alt={value.alt}
						loading="lazy"
						class="absolute inset-0 h-full w-full object-cover object-center transition-transform duration-700 ease-out will-change-transform group-hover:scale-[1.04]"
					/>
				</div>
				<div class="p-5 sm:p-6">
					<h3 class="font-serif text-2xl font-medium leading-snug text-pine-teal-200">{value.title}</h3>
					<p class="mt-3 text-sm leading-relaxed text-pine-teal-400">{value.desc}</p>
				</div>
			</div>
		))}
	</div>
</section>
